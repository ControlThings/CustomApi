<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="mist.request">

    <application android:allowBackup="true" android:label="@string/app_name"
        android:supportsRtl="true">

        <service
            android:name="mist.MistService"
            android:exported="false"
            android:stopWithTask="true"/>

    </application>

</manifest>
